import React from 'react';
import useTitle from '../../hooks/useTitle';

const Settings = () => {
    useTitle("SCOFS - Settings");

    return (
        <div>
            <h1 className='text-center text-3xl'>Settings page</h1>

        </div>
    );
};

export default Settings;